Это нормально: команда ничего не пишет, если выполнилась успешно.

Теперь только проверим, что имя сохранилось. Выполни:

```bash
git config --global user.name
```

Должно вывести:

```text
theincrediblesheva1215-dev
```

Пришли, что показало.
